# vim: filetype=swayconfig

# AUTOSTART
# ------------------------
exec sleep 5 && systemctl start --user sway-session.target

# Polkit agent
# exec /usr/bin/lxpolkit # Hard dependency on elogind?

# Keyring daemon
# this must connect to --login daemon started by pam?
# see https://wiki.archlinux.org/title/GNOME/Keyring#Using_the_keyring
# exec gnome-keyring-daemon --start --components=ssh,secrets

# Sailing
# exec transmission-killswitch

# Audio
# exec pipewire
# exec wireplumber
# exec pipewire-pulse

# Adjust gamma per time of day (ala redshift)
# exec gammastep

# Status window
# exec ustatus status

# Overview
# exec rm -f /tmp/sovpipe && mkfifo /tmp/sovpipe && tail -f /tmp/sovpipe | sov

# Applets
# exec nm-applet --indicator
# exec blueman-applet
# exec udiskie --no-automount --tray
# ------------------------
